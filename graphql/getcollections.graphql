query getCollections($accountId: String!) {
  Library {
    collection {
      collections(accountId: $accountId) {
        name
        collectionId
        libraryItemContentVersion
      }
    }
  }
}